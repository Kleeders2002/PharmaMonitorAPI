# Database
DATABASE_URL=postgresql://user:password@localhost/PharmaMonitorDB

# JWT Secret
JWT_SECRET=your-secret-key-here-change-in-production

# ============================================================
# NUEVA ARQUITECTURA BIDIRECCIONAL (2024)
# ============================================================
# El NodeMCU ahora envía datos al backend vía POST
# Ya NO se requiere exponer el NodeMCU a internet (no más ngrok/túneles)

# Sensor Configuration
USE_REAL_SENSORS=true      # true=recibir datos reales del NodeMCU, false=simular datos
SENSOR_STRICT_MODE=true    # true=generar alertas si sensor falla, false=ignorar fallos

# Variables antiguas (ya NO necesarias - mantener solo para compatibilidad)
# NODEMCU_IP=192.168.0.117
# NODEMCU_PORT=80
# NODEMCU_LED_ENDPOINT=/led
# NODEMCU_DATA_ENDPOINT=/data

# Environment (development/production)
ENVIRONMENT=development

# CORS Origins (comma separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:19006
