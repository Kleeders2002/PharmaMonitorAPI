# ============================================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================================
# Development (local)
DATABASE_URL=postgresql://user:password@localhost/PharmaMonitorDB

# Production (Render) - Render configura esto automáticamente
# DATABASE_URL=postgresql://user:password@host/dbname

# ============================================================
# JWT SECRET
# ============================================================
# IMPORTANTE: Cambiar esto en producción a una cadena segura
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-secret-key-here-change-in-production

# ============================================================
# NUEVA ARQUITECTURA BIDIRECCIONAL (2024)
# ============================================================
# El NodeMCU ahora envía datos al backend vía POST /nodemcu/data
# Ya NO se requiere exponer el NodeMCU a internet (no más ngrok/túneles)

# Configuración de Sensores
USE_REAL_SENSORS=true      # true=recibir datos reales del NodeMCU, false=simular datos
SENSOR_STRICT_MODE=true    # true=generar alertas si sensor falla, false=ignorar fallos

# Variables antiguas (OBSOLETAS - ya NO se usan)
# NODEMCU_IP=192.168.0.117       # Ya NO necesaria
# NODEMCU_PORT=80                # Ya NO necesaria
# NODEMCU_LED_ENDPOINT=/led      # Ya NO necesaria
# NODEMCU_DATA_ENDPOINT=/data    # Ya NO necesaria

# ============================================================
# ENTORNO
# ============================================================
ENVIRONMENT=development  # development | production

# Production URLs (ejemplo para Render)
# BACKEND_URL=https://pharmamonitor-api.onrender.com
# FRONTEND_URL=https://pharmamonitor-frontend.onrender.com

# ============================================================
# CORS ORIGINS (comma separated)
# ============================================================
# Development
CORS_ORIGINS=http://localhost:3000,http://localhost:19006,http://localhost:8081

# Production (descomentar y actualizar con frontend real)
# CORS_ORIGINS=https://pharmamonitor-frontend.onrender.com,https://tu-front-end.com
